#include <stdio.h>

int main(void)
{
    int i;      // Holds signed integers, e.g. -3, -2, 0, 1, 10
    float f;    // Holds signed floating point number, e.g. -3.1416

    printf("Hello, World\n");   // Ah, blessed familiarity
}